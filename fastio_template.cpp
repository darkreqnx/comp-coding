#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    // "\n" instead of endl -- since endl flush won't be required for new line for large outputs
    
    return 0;
}
